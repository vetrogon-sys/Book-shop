server:
  port: 8082

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:5442/${MYSQL_DATABASE}
    username: user
    password: pass
    driver-class-name: com.mysql.jdbc.Driver